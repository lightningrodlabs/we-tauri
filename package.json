{
  "name": "we-dev",
  "private": true,
  "workspaces": [
    "ui/applet-messages",
    "ui/applet-iframe",
    "ui/libs/*",
    "ui/app",
    "example/ui",
    "site"
  ],
  "scripts": {
    "start": "npm run build:happ && npm run build -w @lightningrodlabs/we-applet && concurrently -k \"npm run start:ui\" \"ADMIN_PORT=10001 npm run start:agent\"",
    "network": "npm run build:happ && npm run build -w @lightningrodlabs/we-applet && concurrently \"npm run start:ui\" \"ADMIN_PORT=10001 npm run start:agent\" \"DISABLE_DEEP_LINK=true ADMIN_PORT=10002 npm run start:agent\"",
    "start:ui": "concurrently -k \"npm run build:watch -w @lightningrodlabs/we-applet\" \"npm run start -w we\"",
    "start:agent": "concurrently \"npm run tauri:dev\" \"holochain-playground ws://localhost:${ADMIN_PORT}\" \"npm run publish:applets\"",
    "tauri:dev": "RUST_LOG=info WASM_LOG=warn npm run tauri dev --release -- -- -- --profile ${ADMIN_PORT}",
    "publish:applets": "run-singleton \"node scripts/publish-applets.js\"",
    "build:libs": "npm run build -w @lightningrodlabs/we-applet -w applet-messages -w applet-iframe -w grapes-editor -w @lightningrodlabs/attachments",
    "build:ui": "npm run build:libs && npm run build -w we",
    "test": "npm run build:happ && cargo nextest run -j 1",
    "tauri": "tauri",
    "build:happ": "npm run build:zomes && hc app pack ./workdir --recursive",
    "build:zomes": "cargo build --release --target-dir target --target wasm32-unknown-unknown --workspace --exclude we",
    "build:example": "npm run build:libs && npm run build:zomes && npm run package -w example-applet && rimraf testing-applets/example-applet && cp example/workdir/example-applet.webhapp testing-applets ",
    "prepare:lair": "node scripts/prepare-lair.js",
    "prepare": "curl -L --output DevHub.webhapp https://github.com/holochain/devhub-gui/releases/download/v0.12.0/DevHub.webhapp && npm run build:libs"
  },
  "devDependencies": {
    "@holochain-playground/cli": "^0.1.1",
    "@tauri-apps/cli": "^1.2.3",
    "concurrently": "^6.2.1",
    "concurrently-repeat": "^0.0.1",
    "cross-env": "^7.0.3",
    "js-yaml": "^4.1.0",
    "new-port-cli": "^1.0.0",
    "rimraf": "^4.4.0",
    "run-singleton-cli": "^0.0.7"
  },
  "type": "module"
}
